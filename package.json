{
  "name": "nimbus",
  "private": true,
  "workspaces": [
    "nimbus-cli",
    "nimbus-cdk"  
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "start": "cd nimbus-cli && npm run start"
  }
}

